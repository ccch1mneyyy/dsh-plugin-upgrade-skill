---
kind: dsh-version-card-set
schema: 1
from: dsh-v0.1.2-alpha.5
to: dsh-v0.1.2-rc.1
status: reviewed
coverage: curated
cardCount: 0
idPrefix: DSH-0.1.2-RC1
verifiedAt: 2026-09-03
---

# Change cards · 0.1.2-rc.1

> This file is a curated list of plugin-relevant changes, not a complete API diff.
>
> Upstream comparison: [dsh-v0.1.2-alpha.5...dsh-v0.1.2-rc.1](https://github.com/deepseek-ai/deepseek-harness/compare/dsh-v0.1.2-alpha.5...dsh-v0.1.2-rc.1)
> · Release Notes: [dsh-v0.1.2-rc.1](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.2-rc.1)
> · Tags: `dsh-v0.1.2-alpha.5` = `db6bdc3576`, `dsh-v0.1.2-rc.1` = `a66e470204` (2 commits apart)
> · Card format: see [references/README.md](README.md)
> · Touchpoint numbering: see [pre-flight.md](pre-flight.md)

## Contents

No cards for this corridor: the alpha.5 → rc.1 edge introduces no plugin-facing changes —
the tag contains nothing but a repo-wide version bump. The sections below are the
verification record and a coverage matrix that maps the rc.1 release notes (which summarize
the whole 0.1.1-rc.2 → 0.1.2-rc.1 span, not this edge) onto the existing card set, so future
migrations can skip the diff work and reviewers can see at a glance what is carded and what
is not.

## Corridor finding: no plugin-facing changes

- **Scale**: 2 commits between the tags — `5a69ba1cd` (merge of the alpha.5 release branch)
  and `a66e47020` (`release(dsh): 0.1.2-rc.1`, 2026-09-03). 252 files changed, every one a
  `package.json` version bump from `0.1.2-alpha.5` to `0.1.2-rc.1` (249 under `packages/`,
  2 under `apps/`, plus the root manifest); **zero non-`package.json` source changes**.
  Sampled diffs confirm the `version` field is the only difference.
- **Release-notes nature**: the rc.1 notes summarize the major user- and developer-facing
  changes since `0.1.1-rc.2` (the whole 0.1.2 series). They are not an incremental record of
  this edge — everything they announce already exists at alpha.5 and is covered by the
  corridor cards below.
- **Per-package verification**: with zero source changes between the tags, every runtime API
  surface third-party plugins touch (`dsh-tools`, `dsh-llm`, `dsh-agent`, `dsh-session`,
  `dsh-storage*`, Remote wire, presets, `cordis.patch.yml`, `tool-cordis` catalog) is
  identical to `dsh-v0.1.2-alpha.5`, whose own verification record ([v0.1.2-alpha.5.md](v0.1.2-alpha.5.md),
  the alpha.4→alpha.5 corridor card set) therefore carries over unchanged.
- **npm channels** (measured 2026-09-03): `next` = `0.1.2-rc.1` (RCs ride the `next`
  channel); `alpha` = `0.1.2-alpha.5`; `latest` = `0.1.1-rc.2` for the umbrella
  `@deepseek-ai/dsh` only — sub-packages keep `latest` at `0.0.1-rc.1` / `0.1.0-rc.6`, so
  install sub-packages with an explicit version or the `alpha`/`next` tag.
- **Meaning for migrations**: rc.1 is the first release candidate of the 0.1.2 series. A
  plugin migrating to `0.1.2-rc.1` from any earlier release follows the existing corridor
  chain — [v0.1.2-alpha.1.md](v0.1.2-alpha.1.md) (rc.2→alpha.1) → alpha.2 → alpha.3 →
  alpha.4 → the alpha.4→alpha.5 card set ([v0.1.2-alpha.5.md](v0.1.2-alpha.5.md)) → this file — and lands on
  the same code it would have hit at alpha.5. Everything in the rollup remains in force,
  one step closer to the final-release review.

## Release-notes coverage matrix (rc.1 summary × corridor cards)

Developer- and plugin-relevant items from the rc.1 notes, mapped to the card or rollup
entry that already covers them. "Carded" means the item is explained with a migration
recipe and verification somewhere in this corridor; "gap" means the item is not carded and
a backfill candidate is recorded below.

| rc.1 release-notes item | Where carded | Coverage note |
|---|---|---|
| `send_message` replaces the one-way `report` tool (parent ↔ continuable child) | [DSH-0.1.2-A4-01](v0.1.2-alpha.4.md) | breaking, tool-package removal |
| Remote gateway unifies remote calls/errors; legacy APIProxy migrated and removed | [DSH-0.1.2-A1-01](v0.1.2-alpha.1.md) | breaking, wire ledger |
| `Session.events` replaced by `seq` / `eventAt()` / `snapshotEvents()` | [DSH-0.1.2-A4-03](v0.1.2-alpha.4.md) | breaking, read-API ledger |
| `SessionSeq` / `SessionLogOffset` strong types (forward compatible) | [DSH-0.1.2-A4-04](v0.1.2-alpha.4.md) | breaking (typecheck surface) |
| Public WebFetch enabled by default (SSRF protection; no per-request approval) | [DSH-0.1.2-A1-07](v0.1.2-alpha.1.md) | behavior/security default |
| Python SDK / Headless / ACP / custom profiles get `web_fetch` by default | [DSH-0.1.2-A4-06](v0.1.2-alpha.4.md) | behavior default (base bundle) |
| Web PTC Mode no longer exposes the general `workflow` tool | [DSH-0.1.2-A4-05](v0.1.2-alpha.4.md) | behavior default (preset) |
| Code Mode renamed to PTC mode; old session records still read | [DSH-0.1.2-A1-06](v0.1.2-alpha.1.md) | behavior |
| pi-ai model support update (0.82 → 0.84 dependency) | [DSH-0.1.2-A1-24](v0.1.2-alpha.1.md) | packaging: duplicate-instance trap; vLLM thinking-budget config is a new optional provider option (capability, no break) |
| npm peer-dependency cleanup | [DSH-0.1.2-A2-03](v0.1.2-alpha.2.md) | packaging |
| Node.js 24.0–24.11.1 startup/HMR fix | [DSH-0.1.2-A2-04](v0.1.2-alpha.2.md) | fix |
| Plugin inventory groups by session/global plugin and per Agent Preset | [DSH-0.1.2-A2-05](v0.1.2-alpha.2.md) | additive inventory field; the grouping/preset-switcher UI is web-client surface |
| Model settings page can host provider login configuration | [DSH-0.1.2-A1-09](v0.1.2-alpha.1.md) | capability (provider plugins) |
| Third-party language registration | [DSH-0.1.2-A1-10](v0.1.2-alpha.1.md) | capability (UI plugins) |
| Subagent model selection: authorized self-choice or caller-specified provider/model/effort/max output; model config for Claude Code / Codex | [DSH-0.1.2-A1-11](v0.1.2-alpha.1.md) | capability |
| Official DeepSeek adapter sends enabled-plugin names/versions (configurable off) | [DSH-0.1.2-A1-12](v0.1.2-alpha.1.md) | privacy default |
| Optional incremental Session log upload (default off) | [DSH-0.1.2-A1-14](v0.1.2-alpha.1.md) | privacy, opt-in |
| Headless streams progress to stderr; stdout carries the final result | [DSH-0.1.2-A1-05](v0.1.2-alpha.1.md) | behavior |
| Everything launches through a `dsh` profile (Python SDK, ACP included) | [DSH-0.1.2-A1-04](v0.1.2-alpha.1.md) | packaging/behavior |
| Persistent-shell fixes (macOS/Linux bash/pwsh startup, pipe reads, macOS host stall under many child processes); Windows directory-picker path truncation | [DSH-0.1.2-A1-13](v0.1.2-alpha.1.md) | fixes; check old platform workarounds |
| Profile-configured Agent Preset directory lost at boot; unloadable presets flagged early | rollup [R-10](rollup-0.1.2.md) / [DSH-0.1.2-A1-21](v0.1.2-alpha.1.md) | fix on the `roots`/`includeShippedRoot` surface |
| WebSocket heartbeats avoid idle-connection drops | rollup / [DSH-0.1.2-A1-08](v0.1.2-alpha.1.md) | transport robustness; raw-client lanes benefit, no break |
| Web search failure reports the actual endpoint and error | — | informational behavior; no plugin migration needed |
| File-edit tools accept `null` placeholders for fields unused by the current operation | — | informational; host-tool tolerance, see gaps below |
| Experimental Inspector tool and Web Preview | — | capability surface, see gaps below |
| SQLite Session persistence backend removed (existing content kept; export from an older version first) | — | **gap**, see gaps below |

## Backfill candidates (not part of this edge)

Three items from the rc.1 summary are **not carded anywhere** in the corridor. None is a
breaking change on this edge (rc.1 differs from alpha.5 by nothing), so they are recorded
here as candidates for older-edge backfills rather than as rc.1 cards:

1. **SQLite Session-persistence backend removal** — a profile/configuration surface change
   that shipped earlier in the 0.1.2 series (the alpha.2 → alpha.3 edge carded zero cards
   and did not record it). Profiles that route session persistence to the sqlite backend
   must export/rebuild before upgrading; the alpha.2 card [DSH-0.1.2-A2-01](v0.1.2-alpha.2.md)
   already notes the sqlite provider's schema-20-only stance, but the backend's removal is
   not carded. Candidate: an alpha.3-edge backfill card (same class as the open
   alpha.3 settings-card backfill), stating when the removal landed and the export path.
2. **File-edit tool `null` placeholder tolerance** — host-tool schema tolerance, low impact
   for plugin authors; informational only.
3. **Experimental Inspector / Web Preview** — new capability surfaces; plugins that want to
   integrate them should check the rc.1 tag's packages directly (no card because no
   migration is required and the surface is experimental).

## Conclusion

Treat the alpha.5 → rc.1 edge as a no-op corridor: same code, new version stamp, `next`
channel. The 0.1.2 knowledge base (alpha.1 → alpha.5 cards plus the rollup) is fully
applicable to the first release candidate; the rollup's final-release review is now one
step away. If the backfill candidates above are adopted, the rc.1 summary will be fully
mapped.
